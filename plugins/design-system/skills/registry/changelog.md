# Changelog

| Дата | Тип | Суть обращения | Что сделано | Результат |
|------|-----|----------------|-------------|-----------|
| 2026-08-31 | новая фича | Написать скилл `registry` и генератор просмотра реестра (Task 4 плана) | Созданы `SKILL.md`, четыре справочника (`atomic-levels`, `cross-cutting`, `flows`, `page-templates` — каталог из 29 паттернов), генератор `build-registry-html.mjs` и валидатор `check-registry.mjs`, пересчитывающий заявленное по коду проекта. Валидатор проверен 13 мутациями на контрольном проекте — каждая уронила проверку, чистый прогон даёт код 0. | Выполнено |
